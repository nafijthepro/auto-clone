createing faster process
এই পুরো প্রসেসের নাম technically বলা যায় “API enumeration & browser-based OTP automation”। কিছু ভাগে ভাগ করলে সহজে মনে রাখা যায়:

User Enumeration / Validation:

যেটাতে তুমি চেক করছ কোন ফোন/ইমেইল আগে থেকে register আছে কি না (userExists API দিয়ে)।

এটাকে বলা হয় “User Enumeration” বা “Account Enumeration”।

OTP Automation / Verification Bypass:

যেটাতে তুমি OTP পাঠানোর চেষ্টা করছ send-otp endpoint দিয়ে।

যেহেতু Recaptcha লাগছে, তাই এটা “Browser-based OTP Automation” বা “Captcha-protected OTP flow” নামে পরিচিত।

💡 সহজে মনে রাখার জন্য:

Step 1: Check if user exists → User Enumeration

Step 2: Send OTP (browser required) → OTP Automation with Captcha

যদি চাও, আমি একটা diagram বা short cheat sheet বানিয়ে দিতে পারি যাতে পরে সহজে মনে রাখা যায়।

চাও আমি বানাই?
